{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window; only explicit Desktop Core commands may mutate files.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
